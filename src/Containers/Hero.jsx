import { Carousel, HeroBanner, MultiCarousel } from "../Components";

const Hero = () => {
  return (
    <section className="mt-[100px]">
      <Carousel />
      <HeroBanner />
      <MultiCarousel />
    </section>
  );
};

export default Hero;
